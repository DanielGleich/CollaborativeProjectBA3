using UnityEngine;

public class Ballast : MonoBehaviour
{
    [field: Header("Settings")]
    [field: SerializeField] public float Weight {get; private set;}

    // More code migth follow later...
}
